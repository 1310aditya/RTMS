<%= form_for(@p_sub_cat) do |f| %>
  <% if @p_sub_cat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@p_sub_cat.errors.count, "error") %> prohibited this p_sub_cat from being saved:</h2>

      <ul>
      <% @p_sub_cat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :desc %><br>
    <%= f.text_area :desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
