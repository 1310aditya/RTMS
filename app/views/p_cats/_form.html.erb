<%= form_for(@p_cat) do |f| %>
  <% if @p_cat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@p_cat.errors.count, "error") %> prohibited this p_cat from being saved:</h2>

      <ul>
      <% @p_cat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Title_of_Category %><br>
    <%= f.text_field :title, :placeholder => "Title of Category" %>
  </div><br><br>
  <div class="field" style="width:300px, height:90px">
    <%= f.label :Description %><br>
    <%= f.text_area :desc, :placeholder => "Description of Category"%>
  </div><br><br>
  <div class="actions">
    <%= f.submit :Create_Category%>
  </div>
<% end %>
