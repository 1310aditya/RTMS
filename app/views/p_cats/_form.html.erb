<%= form_for(@p_cat) do |f| %>
  <% if @p_cat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@p_cat.errors.count, "error") %> prohibited this p_cat from being saved:</h2>

      <ul>
      <% @p_cat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :desc %><br>
    <%= f.text_area :desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
